import { selectionEditingConfig } from 'app/machines/explore/selectionEditing.config';

export const routeConfig = {
	id: 'ExploreRoute',
	// initial: 'Loading',
	initial: 'Interactive',
	on: {
	},
	states: {
		Loading: {
			entry: ['logState'],
			on: {
				READY: {
					target: '#ExploreRoute.Interactive',
					actions: ['logState']
				}
			}
		},
		Interactive: {
			type: 'parallel',
			states: {
				SelectionEditing: selectionEditingConfig,
			},
			entry: ['logState'],
			on: {
				ROUTE_CHANGED: {
					target: '#ExploreRoute.Interactive',
					actions: ['logState']
				}
			}
		}
	}
};
