import {selectingConfig} from 'app/machines/explore/selecting.config';
import {historyConfig} from './history.config';

export const routeConfig = {
	id: 'ExploreRoute',
	initial: 'Interactive',
	on: {
	},
	states: {
		Interactive: {
			type: 'parallel',
			states: {
				History: historyConfig,
				Selecting: selectingConfig,
			},
			on: {
				DATASET_UPDATED: {
					target: '#ExploreRoute.Interactive',
					actions: ['storeDataset']
				},
			}
		}
	}
};
