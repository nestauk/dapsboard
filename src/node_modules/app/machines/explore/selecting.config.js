export const selectingConfig = {
	initial: 'Idle',
	states: {
		Idle: {
			entry: ['logEvent'],
			on: {
				SELECTED_FIELDS: {
					target: 'Idle',
					actions: ['selectFields', 'sendEdited'],
				},
				CLICKED_FIELD_COUNTER: {
					target: 'Idle',
					actions: ['toggleField', 'sendEdited']
				},
				SELECTED_NEXT_FIELD: {
					target: 'Idle',
					actions: ['selectNextField', 'sendEdited']
				},
				SELECTED_PREVIOUS_FIELD: {
					target: 'Idle',
					actions: ['selectPreviousField', 'sendEdited']
				},
			}
		}
	}
};
