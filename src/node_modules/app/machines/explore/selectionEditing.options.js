import {stringify} from '@svizzle/utils';
import {actions} from 'xstate';

const {log} = actions;

export const selectionEditingOptions = {
	actions: {
		/**
		 * Debug
		 */
		logState: log(
			(context, event) => `event: ${stringify(event)}, context: ${stringify(context)}`,
			'logState'
		)
	},
	guards: {
	}
};
