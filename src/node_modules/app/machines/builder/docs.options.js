const DEFAULT_FIELD_DOCS = 'Click on a field for docs.';
const DEFAULT_AGG_DOCS = 'Hover on an aggregation for short description or click on the outgoing link for full reference.';

export const docsOptions = {
	actions: {
		/**
		 * Sets the field doc string to be displayed in the UI
		 */
		setFieldDoc: (ctx, {docstring}) => ctx.activeDocs.update(() => docstring),
		/**
		 * Resets the field doc string to the default text.
		 */
		resetFieldDoc: ctx => ctx.activeDocs.update(() => DEFAULT_FIELD_DOCS),
		/**
		 * Sets the agg doc string to be displayed in the UI
		 */
		setAggDoc: (ctx, {docstring}) => ctx.aggDocText.update(() => docstring),
		/**
		 * Resets the agg doc string to the default text.
		 */
		resetAggDoc: ctx => ctx.aggDocText.update(() => DEFAULT_AGG_DOCS),
	},
	guards: {
	}
};
